# Nlargest function

`nlargest` — это один из тех инструментов в Pandas, который делает код чище и производительнее одновременно.

Вот краткая шпаргалка, которую можно сохранить:

---

### 📝 Заметка: Метод `.nlargest()` в Pandas

**Зачем нужен:**
Позволяет быстро найти строки с самыми большими значениями в указанном столбце. Это более эффективная альтернатива связке `.sort_values().head()`.

**Синтаксис:**

```python
df.nlargest(n, columns, keep='first')

```

**Основные параметры:**

1. **`n`**: Количество строк, которые нужно вернуть.
2. **`columns`**: Имя столбца (или список имен), по которому ищем максимум.
3. **`keep`** (самое важное):
* `'first'` (по умолчанию): если есть одинаковые значения, возьмет того, кто встретился первым.
* `'last'`: возьмет того, кто встретился последним.
* `'all'`: **аналог `WITH TIES` в SQL**. Оставит всех дубликатов, если их значения равны «граничному» результату. Именно это мы использовали в твоей задаче.



---

### Почему стоит использовать именно его?

1. **Производительность:** Вместо полной сортировки всего DataFrame (сложность ), `nlargest` использует алгоритм выбора (на базе кучи/heap), что работает значительно быстрее на больших данных (, где  — количество искомых строк).
2. **Читаемость:** Код сразу говорит о намерении: «мне нужны  самых больших», не заставляя читать параметры сортировки.
3. **Гибкость:** Параметр `keep='all'` избавляет от написания дополнительных фильтров для поиска всех строк с максимальным значением.

---

### Пример использования (best practice):

```python
# Найти 3-х сотрудников с самой высокой зарплатой, включая всех "равных"


top_employees = df.nlargest(3, 'salary', keep='all')

```

**Когда НЕ использовать:**
Если тебе нужно найти минимальные значения — используй зеркальный метод **`.nsmallest()`**.

# PCT_CHANGE
Вот короткие заметки (cheat sheet) по оптимизации твоего Pandas-кода:

### 1. Избегай `apply(axis=1)`

* **Проблема:** `apply` запускает цикл Python, который перебирает строки по одной. Это очень медленно на больших данных.
* **Решение:** Используй **векторизированные операции**. Pandas умеет складывать и делить целые колонки мгновенно (используя C и NumPy под капотом).
* **Пример:** Вместо `apply(lambda x: x['a'] / x['b'])` пиши `df['a'] / df['b']`.

### 2. Используй специализированные методы (`pct_change`)

* **Проблема:** Ручной расчет `(value - shift) / shift` громоздок и требует создания лишних временных колонок.
* **Решение:** Метод `.pct_change()` делает это одной командой.
* **Бонус:** Код становится читаемее для других аналитиков.

### 3. `resample()` вместо `groupby()` для дат

* **Проблема:** `groupby()` просто группирует то, что есть. Если данных за Февраль нет, он сравнит Март с Январем, и MoM (Month-over-Month) будет рассчитан неверно.
* **Решение:** `.resample('MS')` (Month Start) создает непрерывную временную шкалу. Если месяца нет в данных, он создаст для него пустую строку (которую можно заполнить 0), и статистика не "сломается".

### 4. Округление "на лету"

* **Совет:** Метод `.round(2)` можно вызывать цепочкой сразу после вычислений. Это избавляет от лишних строк кода.

---

**Хочешь покажу на конкретном примере данных, как `groupby` выдаст ошибку логики при пропущенном месяце, а `resample` это исправит?**
